# Email Worker Service Dependencies
celery==5.3.4
redis==5.0.1
google-api-python-client==2.108.0
google-auth-httplib2==0.1.1
google-auth-oauthlib==1.1.0
google-auth==2.23.4
beautifulsoup4==4.12.2
html2text==2020.1.16
trafilatura==2.0.0
httpx==0.25.2
aiohttp==3.9.1
pydantic==2.5.0
python-dotenv==1.0.0
lxml==5.3.0
Pillow==10.4.0
# Used only by services/email-worker/scripts/backfill_text_dominant_heroes.py
# (operational tool that runs ad-hoc against dev/prod DBs). Worker runtime
# never imports psycopg2 — DB access in tasks goes through the data-server.
psycopg2-binary==2.9.10

# IANA timezone database for zoneinfo (stdlib) on python:*-slim images where
# /usr/share/zoneinfo is absent. Required for per-user local-time scheduling.
tzdata==2024.1

# Dev / test — harmless in prod but only exercised by pytest
pytest==8.3.3